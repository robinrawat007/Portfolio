[build]
  command = "npm run build"

# Full Next.js (API routes at /api/*). Do not publish `out` — static export forces
# client-side n8n webhooks and triggers CORS failures for Atlas in the browser.
[[plugins]]
  package = "@netlify/plugin-nextjs"
